#pragma once

#include "../libretro-arb/libretro.h"

namespace Retro
{
  enum K
  {
    UNKNOWN=RETROK_UNKNOWN,
    FIRST=RETROK_FIRST,
    BACKSPACE=RETROK_BACKSPACE,
    TAB=RETROK_TAB,
    CLEAR=RETROK_CLEAR,
    RETURN=RETROK_RETURN,
  };
};
